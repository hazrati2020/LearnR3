# Basics of R -------------------------------------------------------------

weight_kilos <- 100
colnames(airquality)
str(airquality)
summary(airquality)
1 + 1
mean(2:6)
æ

# loading packages --------------------------------------------------------

library(tidyverse)
r3::check_git_config()
r3::setup_git_config()
